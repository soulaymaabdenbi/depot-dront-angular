import { AgeValidationDirective } from './age-validation.directive';

describe('AgeValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeValidationDirective();
    expect(directive).toBeTruthy();
  });
});
